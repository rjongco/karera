import * as React from "react";

interface INotAuthenticatedProps {}

const NotAuthenticated: React.FunctionComponent<
  INotAuthenticatedProps
> = () => {
  return <div>Not Authenticated</div>;
};

export default NotAuthenticated;
